using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerAnimator : CharacterAnimator
{
    //public AnimationClip spellStartRight;
    //public AnimationClip spellStartLeft;

    /*public void OnSpellChange(Spell spell, bool rightHand)
    {
        if (rightHand == true)
        {
            overrideController["spell_right_start"] = spellStartRight;
            overrideController["spell_right_cast"] = spell.castingAnimRight;
        }
        else
        {
            overrideController["spell_right_start"] = spellStartLeft;
            overrideController["spell_right_cast"] = spell.castingAnimLeft;
        }
    }*/
}
